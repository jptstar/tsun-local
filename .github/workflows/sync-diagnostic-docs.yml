name: Sync diagnostic documentation

on:
  push:
    branches:
      - main
    paths:
      - "scripts/publish_macos_first_launch_docs.py"
      - ".github/workflows/sync-diagnostic-docs.yml"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: sync-diagnostic-docs
  cancel-in-progress: true

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Publish safe macOS first-launch guidance
        run: python scripts/publish_macos_first_launch_docs.py

      - name: Verify stable links and generated documentation
        run: |
          python -m py_compile scripts/publish_macos_first_launch_docs.py
          grep -q 'releases/download/diagnostic-latest/TSUN-Local-Diagnostic.exe' README.md
          grep -q 'TSUN-Local-Diagnostic-macOS-arm64.zip' README.md
          grep -q 'TSUN-Local-Diagnostic-macOS-x86_64.zip' README.md
          grep -q 'Open Anyway' README.md
          grep -q 'TSUN-Local-Diagnostic-Linux-x86_64' docs/HARDWARE_DUMP.md
          grep -q 'Mac M1 / M2 / M3 / M4' docs/test-your-inverter.html
          grep -q 'releases/download/diagnostic-latest/TSUN-Local-Diagnostic-macOS-arm64.zip' docs/test-your-inverter.html
          grep -q 'releases/download/diagnostic-latest/TSUN-Local-Diagnostic-macOS-x86_64.zip' docs/test-your-inverter.html
          grep -q 'Open Anyway' docs/test-your-inverter.html
          grep -q 'Ouvrir quand même' docs/README_FR.md
          grep -q 'Gatekeeper' docs/README_FR.md
          test "$(grep -c 'Mac &amp; Linux diagnostic →' docs/index.html)" -eq 1

      - name: Publish synchronized docs branch
        shell: bash
        run: |
          if git diff --quiet; then
            echo "Documentation already synchronized."
            exit 0
          fi
          branch="automation/diagnostic-docs"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git checkout -B "$branch"
          git add \
            README.md \
            tools/README.md \
            docs/HARDWARE_DUMP.md \
            docs/DIRECT_DIAGNOSTIC_UPLOAD_TEST.md \
            docs/README_*.md \
            docs/index.html \
            docs/test-your-inverter.html
          git commit -m "Publish Mac downloads with first-launch guide"
          git push --force origin "$branch"
          echo "Synchronized documentation published to $branch for pull-request validation."
